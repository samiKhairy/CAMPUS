<?xml version="1.0" encoding="UTF-8" ?>
<CycloneDDS xmlns="https://cdds.io/config"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
    <Domain Id="any">
        <General>
            <!-- Docker bridge networks don't reliably support multicast.
                 Force unicast discovery so all DDS participants on the
                 same Docker bridge can find each other via SPDP. -->
            <AllowMulticast>spdp</AllowMulticast>
            <MaxMessageSize>65500</MaxMessageSize>
        </General>
        <Discovery>
            <ParticipantIndex>auto</ParticipantIndex>
            <!-- Localhost peer covers all participants on the same
                 Docker bridge network (they share a subnet). The
                 DDS SPDP protocol will enumerate participant indices
                 0-119 on each listed peer address. -->
            <MaxAutoParticipantIndex>120</MaxAutoParticipantIndex>
        </Discovery>
        <!-- <Internal>
            <SocketReceiveBufferSize min="1MB" />
        </Internal> -->
    </Domain>
</CycloneDDS>
